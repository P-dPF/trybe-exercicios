type Bird = {
  color: string;
  ageSpan: number;
};

type Sum = (firstNum: number, secondNum: number) => number;

type Address = {
  street: string;
  number: number;
  city: string;
  country: string;
};